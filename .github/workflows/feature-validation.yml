name: Feature validation

on:
  push:
    branches:
      - feature/detailed-power-vector-plots

jobs:
  acceptance:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install test dependencies
        run: python -m pip install -e '.[dev]'
      - name: Run repository acceptance
        run: bash tools/ari-emonio-acceptance.sh
      - name: Remove temporary workflow from candidate tree
        run: rm .github/workflows/feature-validation.yml
      - name: Build deterministic candidate
        run: python tools/build-release.py
      - name: Verify packaged publication content
        run: |
          rm -rf /tmp/ari-emonio-candidate
          mkdir -p /tmp/ari-emonio-candidate
          unzip -q dist/ARI_Emonio_Viewer_v0.4.5_Candidate.zip -d /tmp/ari-emonio-candidate
          python /tmp/ari-emonio-candidate/ARI_Emonio_Viewer_v0.4.5_Candidate/tools/publication_gate.py --working-tree
      - name: Upload candidate
        uses: actions/upload-artifact@v4
        with:
          name: ARI_Emonio_Viewer_v0.4.5_Candidate
          path: |
            dist/ARI_Emonio_Viewer_v0.4.5_Candidate.zip
            dist/ARI_Emonio_Viewer_v0.4.5_Candidate.zip.sha256
          if-no-files-found: error
          retention-days: 7
